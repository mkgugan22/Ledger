# Copy this file to .env and fill in your real values.
# .env is gitignored — never commit your actual connection string.

MONGODB_URI=mongodb+srv://mkgugan22_db_user:CAE25WW5ChjKPyWG@cluster0.me2acck.mongodb.net/
PORT=5000

# Comma-separated list of origins allowed to call this API.
# During local dev, the Vite client runs on http://localhost:5173
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=replace-with-a-long-random-secret
NODE_ENV=development
